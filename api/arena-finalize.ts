import {createClient} from '@supabase/supabase-js';
type Req={headers:Record<string,string|string[]|undefined>};type Res={status:(code:number)=>Res;json:(body:unknown)=>void;setHeader:(name:string,value:string)=>void};
export default async function handler(req:Req,res:Res){res.setHeader('Cache-Control','no-store');const secret=process.env.CRON_SECRET??'',h=req.headers.authorization,auth=Array.isArray(h)?h[0]:h??'';if(secret&&auth!==`Bearer ${secret}`)return res.status(401).json({error:'Unauthorized'});const url=process.env.NEXT_PUBLIC_SUPABASE_URL??process.env.VITE_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY??'';if(!url||!key)return res.status(503).json({error:'Arena database configuration is unavailable.'});const client=createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}}),result=await client.rpc('cb_finalize_arena_sessions');if(result.error)return res.status(500).json({error:result.error.message});return res.status(200).json({ok:true,sessions_finalized:Number(result.data??0),checked_at:new Date().toISOString()});}
